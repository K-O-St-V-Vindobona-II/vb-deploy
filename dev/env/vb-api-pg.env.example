# DE: EnvironmentFile fuer den lokalen vb-api-pg-Dev-Container. Muss zum
#     Passwort in vb-api.env's DATABASE_URL passen.
# EN: EnvironmentFile for the local vb-api-pg dev container. Must match
#     the password in vb-api.env's DATABASE_URL.

POSTGRES_DB=vb
POSTGRES_USER=vb
POSTGRES_PASSWORD=change-me
