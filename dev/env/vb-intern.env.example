# DE: EnvironmentFile fuer den lokalen vb-intern-Dev-Container (`npm run
#     dev`). Volle Doku: vb-intern/.env.example.
# EN: EnvironmentFile for the local vb-intern dev container (`npm run
#     dev`). Full docs: vb-intern/.env.example.

VITE_APP_ENVIRONMENT=development
VITE_API_BASE_URL=http://localhost:20000/api
VITE_PASSWORD_MIN_LENGTH=8
# VITE_GOOGLE_CLIENT_ID=
