# DE: EnvironmentFile fuer den lokalen vb-www-Dev-Container (`npm run dev`).
#     Volle Doku: vb-www/.env.example.
# EN: EnvironmentFile for the local vb-www dev container (`npm run dev`).
#     Full docs: vb-www/.env.example.

VITE_APP_ENVIRONMENT=development
VITE_API_BASE_URL=http://localhost:20000/api
