# Skeleton fuer die Test-Stage - noch kein eigener VPS vorhanden.
# Vor dem ersten Einsatz: Host + alle vier Domains durch echte Werte ersetzen.

[vps]
CHANGEME.example.invalid

[vps:vars]
app_environment=test
api_domain=api-test.CHANGEME.example.invalid
intern_domain=intern-test.CHANGEME.example.invalid
www_domain=www-test.CHANGEME.example.invalid
# S3 API der eigenen MinIO-Instanz (nur Non-Production, siehe README:
# MinIO auf Non-Production-Stages) - oeffentlich erreichbar, da
# generate_presigned_url() vom Browser direkt abgerufene Links erzeugt.
storage_domain=storage-test.CHANGEME.example.invalid
